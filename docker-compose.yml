services:
  # 데이터베이스 컨테이너만 유지
  db:
    image: postgres:15
    container_name: qr_postgres
    environment:
      POSTGRES_USER: tory
      POSTGRES_PASSWORD: nine78&*
      POSTGRES_DB: qrorder
    ports:
      - "5435:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: